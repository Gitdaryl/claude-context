- Sep 19 02:40 ET: Yeti saw the 13 swatch tiles render as alt text in the artifact viewer (house shots from the same folder loaded fine; served bytes identical to local; valid lossy WebP). Could not reproduce in headless chromium. Fix shipped as Version 22: removed loading="lazy" from the tiles and added an on-error fallback that paints the tile's average color (data-hex). If it still shows alt text after a hard reload, the next suspect is the viewer's WebKit handling of images inside <button>; swap to CSS background-image on the button.
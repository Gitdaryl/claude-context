- Sep 19 01:20 ET: wall matcher moved to a compact full-width strip above the color preview (Yeti wanted it in one panel). Found and fixed an id collision: the picker's reason line and the estimator's "why" chips both used id="why", so clicking a wall chip was wiping the estimator chips; picker now uses whyMatch. Artifact Version 19.